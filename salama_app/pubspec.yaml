name: salama_app
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.3.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  google_sign_in: ^6.2.1
  file_picker: ^8.0.0+1           # for file uploads
  uuid: ^4.3.3
  url_launcher: ^6.2.5
  flutter_local_notifications: ^17.0.0  # display local notification 
  awesome_notifications: ^0.9.0
  image_picker: ^1.1.0
 
 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_native_splash: ^2.4.0
  flutter_lints: ^3.0.2
  

flutter:
  uses-material-design: true
  assets:
  - images/logo_200x200.png
    
